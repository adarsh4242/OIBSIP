import React, { useEffect } from "react"; import { createRoot } from "react-dom/client"; import { Provider, useDispatch } from "react-redux"; import { BrowserRouter } from "react-router-dom"; import { Toaster } from "react-hot-toast"; import store from "./app/store"; import { refreshAuth } from "./features/auth/authSlice"; import App from "./App"; import "./index.css";
store.subscribe(() => localStorage.setItem("cart", JSON.stringify(store.getState().cart)));
function Bootstrap() { const dispatch = useDispatch(); useEffect(() => { dispatch(refreshAuth()); }, [dispatch]); return <App />; } createRoot(document.getElementById("root")).render(<Provider store={store}><BrowserRouter><Bootstrap /></BrowserRouter><Toaster position="top-right" /></Provider>);
