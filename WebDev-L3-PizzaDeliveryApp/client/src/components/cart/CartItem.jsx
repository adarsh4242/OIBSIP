import { useDispatch } from "react-redux"; import { decrease, increase, remove } from "../../features/cart/cartSlice"; export default function CartItem({ item }) { const dispatch = useDispatch(); return <div className="flex gap-4 border-b border-black/5 py-4"><img className="h-20 w-20 rounded-xl object-cover" src={item.image} alt="" /><div className="min-w-0 flex-1"><h3 className="font-bold">{item.name}</h3><p className="text-ember">₹{item.price}</p><div className="mt-2 flex items-center gap-3"><button className="btn-muted px-3 py-1" onClick={() => dispatch(decrease(item.product))}>−</button><span>{item.quantity}</span><button className="btn-muted px-3 py-1" onClick={() => dispatch(increase(item.product))}>+</button><button className="ml-auto text-sm text-red-600" onClick={() => dispatch(remove(item.product))}>Remove</button></div></div></div>; }
