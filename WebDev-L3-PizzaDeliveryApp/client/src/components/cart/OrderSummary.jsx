import { Link } from "react-router-dom"; export default function OrderSummary({ subtotal }) { const tax = subtotal * .05; const shipping = subtotal > 999 ? 0 : 99; return <div className="panel"><h2 className="text-xl font-bold">Order summary</h2><div className="mt-5 space-y-3 text-sm"><p className="flex justify-between">Subtotal <b>₹{subtotal.toFixed(2)}</b></p><p className="flex justify-between">Tax <b>₹{tax.toFixed(2)}</b></p><p className="flex justify-between">Shipping <b>{shipping ? `₹${shipping}` : "Free"}</b></p><p className="flex justify-between border-t pt-3 text-lg">Total <b>₹{(subtotal + tax + shipping).toFixed(2)}</b></p></div><Link to="/checkout" className="btn-primary mt-6 block text-center">Checkout</Link></div>; }
