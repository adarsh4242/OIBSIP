import { Link } from "react-router-dom"; import { useDispatch } from "react-redux"; import { add } from "../../features/cart/cartSlice"; import toast from "react-hot-toast";
export default function ProductCard({ product }) { const dispatch = useDispatch(); const price = product.discountPrice || product.price; const rating = Number(product.rating ?? product.ratingsAverage ?? 0); const addItem = () => { dispatch(add({ product: product._id, name: product.name, image: product.images?.[0], price, stock: product.stock, quantity: 1 })); toast.success("Added to cart"); }; return <article className="overflow-hidden rounded-3xl bg-white shadow-sm ring-1 ring-black/5"><Link to={`/products/${product._id}`}><img className="h-48 w-full object-cover" src={product.images?.[0] || "https://images.unsplash.com/photo-1579751626657-72bc17010498?w=800"} alt={product.name} /></Link><div className="p-5"><div className="flex items-start justify-between gap-3"><h3 className="font-bold">{product.name}</h3><span className="font-bold text-ember">₹{price}</span></div><p className="mt-2 line-clamp-2 text-sm text-black/55">{product.description}</p><div className="mt-4 flex items-center justify-between"><span className="text-sm">★ {rating.toFixed(1)} · {product.stock > 0 ? "In stock" : "Sold out"}</span><button disabled={!product.stock} className="btn-primary px-4 py-2 text-sm disabled:cursor-not-allowed disabled:opacity-40" onClick={addItem}>Add</button></div></div></article>; }
