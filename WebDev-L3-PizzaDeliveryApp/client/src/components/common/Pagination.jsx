export default function Pagination({ page, pages, onChange }) { return <div className="mt-6 flex justify-center gap-3"><button className="btn-muted" disabled={page <= 1} onClick={() => onChange(page - 1)}>Previous</button><span className="px-4 py-2">{page} / {pages}</span><button className="btn-muted" disabled={page >= pages} onClick={() => onChange(page + 1)}>Next</button></div>; }
