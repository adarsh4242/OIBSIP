const express = require("express"); const auth = require("../middleware/authMiddleware"); const controller = require("../controllers/orderController"); const router = express.Router(); router.use(auth); router.post("/payment-intent", controller.createPaymentIntent); router.post("/", controller.create); router.get("/my", controller.myOrders); router.get("/:id", controller.getOne); router.put("/:id/pay", controller.markPaid); module.exports = router;
